apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cbt-api.fullname" . }}
  labels:
    {{- include "cbt-api.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{ toYaml .Values.config | nindent 4 }}
