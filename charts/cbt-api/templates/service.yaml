apiVersion: v1
kind: Service
metadata:
  name: {{ include "cbt-api.fullname" . }}
  labels:
    {{- include "cbt-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ include "cbt-api.httpPort" . }}
      targetPort: http
      protocol: TCP
      name: http
  {{- if .Values.extraPorts }}
    {{ toYaml .Values.extraPorts | nindent 4}}
  {{- end }}
  selector:
    {{- include "cbt-api.selectorLabels" . | nindent 4 }}
