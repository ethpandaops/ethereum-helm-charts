apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nimbus-eth2.fullname" . }}-env
  labels:
    {{- include "nimbus-eth2.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.secretEnv }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
