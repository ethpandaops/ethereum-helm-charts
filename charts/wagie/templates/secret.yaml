apiVersion: v1
kind: Secret
metadata:
  name: {{ include "wagie.fullname" . }}-env
  labels:
    {{- include "wagie.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.secretEnv }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
