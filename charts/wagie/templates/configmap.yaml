apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wagie.fullname" . }}
  labels:
    {{- include "wagie.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{ toYaml .Values.config | nindent 4 }}
