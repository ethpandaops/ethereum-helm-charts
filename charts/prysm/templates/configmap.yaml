apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "prysm.fullname" . }}
  labels:
    {{- include "prysm.labels" . | nindent 4 }}
data:
{{- with .Values.config }}
{{- range $key, $value := . }}
  {{ $key }}: |-
    {{- tpl (toString $value) $ | nindent 4 }}
{{- end }}
{{- end }}