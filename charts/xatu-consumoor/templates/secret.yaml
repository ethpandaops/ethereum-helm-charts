apiVersion: v1
kind: Secret
metadata:
  name: {{ include "xatu-consumoor.fullname" . }}-env
  labels:
    {{- include "xatu-consumoor.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.secretEnv }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
