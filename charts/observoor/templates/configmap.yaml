apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "observoor.fullname" . }}
  labels:
    {{- include "observoor.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{ tpl (toYaml .Values.config | nindent 4) $ }}
