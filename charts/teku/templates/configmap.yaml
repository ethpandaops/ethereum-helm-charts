apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "teku.fullname" . }}
  labels:
    {{- include "teku.labels" . | nindent 4 }}
data:
{{- with .Values.config }}
{{- range $key, $value := . }}
  {{ $key }}: |-
    {{- tpl (toString $value) $ | nindent 4 }}
{{- end }}
{{- end }}