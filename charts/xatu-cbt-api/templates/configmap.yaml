apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "xatu-cbt-api.fullname" . }}
  labels:
    {{- include "xatu-cbt-api.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{ toYaml .Values.config | nindent 4 }}
