apiVersion: v1
kind: Secret
metadata:
  name: {{ include "xatu-cbt-api.fullname" . }}-env
  labels:
    {{- include "xatu-cbt-api.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.secretEnv }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
