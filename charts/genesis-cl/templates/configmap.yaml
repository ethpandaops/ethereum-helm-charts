apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "genesis-cl.fullname" . }}
  labels:
    {{- include "genesis-cl.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: |
    {{- $value | nindent 4 }}
{{- end }}
