apiVersion: v1
kind: Service
metadata:
  name: {{ include "genesis-cl.fullname" . }}-headless
  labels:
    {{- include "genesis-cl.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - port: {{ include "genesis-cl.httpPort" . }}
      targetPort: http
      protocol: TCP
      name: http
  {{- if .Values.extraPorts }}
    {{ toYaml .Values.extraPorts | nindent 4}}
  {{- end }}
  selector:
    {{- include "genesis-cl.selectorLabels" . | nindent 4 }}
