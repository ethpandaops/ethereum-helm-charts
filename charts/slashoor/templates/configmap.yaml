apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "slashoor.fullname" . }}
  labels:
    {{- include "slashoor.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{- toYaml .Values.config | nindent 4 }}
