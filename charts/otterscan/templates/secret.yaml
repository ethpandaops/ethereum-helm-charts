apiVersion: v1
kind: Secret
metadata:
  name: {{ include "otterscan.fullname" . }}-env
  labels:
    {{- include "otterscan.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.secretEnv }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
