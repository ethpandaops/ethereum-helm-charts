apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ethereumjs.fullname" . }}
  labels:
    {{- include "ethereumjs.labels" . | nindent 4 }}
data:
  ethereumjs.toml: |
    {{- .Values.config | nindent 4 }}