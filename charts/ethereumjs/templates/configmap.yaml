apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ethereumjs.fullname" . }}
  labels:
    {{- include "ethereumjs.labels" . | nindent 4 }}
data:
{{- with .Values.config }}
{{- range $key, $value := . }}
  {{ $key }}: |-
    {{- tpl (toString $value) $ | nindent 4 }}
{{- end }}
{{- end }}