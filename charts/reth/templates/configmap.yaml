{{- if .Values.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "reth.fullname" . }}-configmap
  labels:
    {{- include "reth.labels" . | nindent 4 }}
data:
  config.toml: |
    {{- tpl .Values.config . | nindent 4 }}
{{- end }}
