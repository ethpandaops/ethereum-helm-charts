apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reth.fullname" . }}-test-connection"
  labels:
    {{- include "reth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: netcat
      image: busybox
      command: ['nc']
      args:
        - -z
        - -v
        - '{{ include "reth.fullname" . }}'
        - '{{ include "reth.p2pPort" . }}'
  restartPolicy: Never
