# Use http-rpc port for readiness probe in CI
# http-rpc binds to 0.0.0.0 and should be accessible
readinessProbe:
  tcpSocket:
    port: http-rpc
  initialDelaySeconds: 60
  periodSeconds: 10

livenessProbe:
  tcpSocket:
    port: http-rpc
  initialDelaySeconds: 120
  periodSeconds: 30