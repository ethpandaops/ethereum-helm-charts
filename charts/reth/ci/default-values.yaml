# Use metrics port for readiness probe in CI tests
# since RPC port takes time to become ready on mainnet
readinessProbe:
  tcpSocket:
    port: metrics
  initialDelaySeconds: 30
  periodSeconds: 10

# Disable file logging for CI tests
fileLogging:
  enabled: false
