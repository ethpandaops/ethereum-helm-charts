apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grandine.fullname" . }}
  labels:
    {{- include "grandine.labels" . | nindent 4 }}
data:
{{- with .Values.config }}
{{- range $key, $value := . }}
  {{ $key }}: |-
    {{- tpl (toString $value) $ | nindent 4 }}
{{- end }}
{{- end }}