apiVersion: v1
kind: Secret
metadata:
  name: {{ include "xatu-sentry-logs.fullname" . }}-env
  labels:
    {{- include "xatu-sentry-logs.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.secretEnv }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
