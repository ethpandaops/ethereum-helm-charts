apiVersion: v1
kind: Service
metadata:
  name: {{ include "benchmarkoor-ui.fullname" . }}
  labels:
    {{- include "benchmarkoor-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "benchmarkoor-ui.selectorLabels" . | nindent 4 }}
