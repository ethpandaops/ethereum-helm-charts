apiVersion: v1
kind: Service
metadata:
  name: {{ include "benchmarkoor-api.fullname" . }}
  labels:
    {{- include "benchmarkoor-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "benchmarkoor-api.selectorLabels" . | nindent 4 }}
