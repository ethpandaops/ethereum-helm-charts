replicas: 1

mode: bootnode

extraArgs:
  - --network=prater

livenessProbe: null

readinessProbe:
  tcpSocket: null
  exec:
    command: ["cat", "/data/beacon/network/enr.dat"]

persistence:
  enabled: true
  accessModes:
  - ReadWriteOnce
  size: 1Gi
  storageClassName: do-block-storage
