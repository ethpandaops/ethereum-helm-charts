replicas: 1

extraArgs:
  - --network=prater
  - --eth1-endpoints=https://rpc.slock.it/goerli

p2pNodePort:
  enabled: true
  startAt: 32000

persistence:
  enabled: true
  accessModes:
  - ReadWriteOnce
  size: 100Gi
  storageClassName: do-block-storage

serviceMonitor:
  enabled: true
